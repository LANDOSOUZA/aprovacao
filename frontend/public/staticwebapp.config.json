{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/.auth/*",
      "/api/*",
      "/*.{css,js,png,jpg,svg,ico}"
    ]
  },
  "routes": [
    {
      "route": "/admin*",
      "allowedRoles": ["authenticated"]
    }
  ]
}